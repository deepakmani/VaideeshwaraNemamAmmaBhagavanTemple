var app = app;

app.controller('addMiracleController', function($scope, $q, flash) {

	// Send Tweet method
	/*$scope.sendTweet = function() {
		console.log("Nemam Amma Bhagavan Sharanam -- sending the tweet");
         TweetAction.tweet($scope.to_twitter_handle, $scope.tweet_text);
	}*/
});